export enum Routes {
  USERS = 'users',
  POSTS = 'posts',
  COMMENTS = 'comments',
  AUTH = 'auth',
  AI = 'ai',
}

export enum UserRoutes {
  USER_ID = ':userId',
  UNFOLLOW_ID = ':unfollowId',
  FOLLOWERS = 'followers',
  FOLLOWING = 'following',
}
